
{% extends '::base.html.twig'  %}

{% block body %}
<div class="container">
    <form action="{{ path('select') }}" method="post" {{ form_enctype(form) }} novalidate >
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-primary" value="Save Post" />
    </form>
</div>
{% endblock %}
